<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.musicfactoryaw">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ClassicalActivity"
            android:label="@string/classical"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".GospelActivity"
            android:label="@string/gospel"
            android:parentActivityName=".MainActivity"/>

        <activity
            android:name=".SlowJamActivity"
            android:label="@string/slowjams"
            android:parentActivityName=".MainActivity"/>

        <activity
            android:name=".JazzActivity"
            android:label="@string/jazz"
            android:parentActivityName=".MainActivity">
        </activity>
    </application>

</manifest>